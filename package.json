{
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rimraf src/.observablehq/cache",
    "build": "observable build",
    "dev": "observable preview",
    "http": "npx http-server dist"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.4",
    "d3-dsv": "^3.0.1",
    "geo-tz": "^8.1.6",
    "odbc": "^2.5.0",
    "suncalc": "^2.0.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=18"
  }
}
